# Test Environment Configuration
# This file contains environment variables specifically for testing

# Database Configuration (in-memory for tests)
DATABASE_URL=sqlite:///:memory:
ASYNC_DATABASE_URL=sqlite+aiosqlite:///:memory:

# Application Environment
ENVIRONMENT=test

# Security Settings (test-only keys)
SECRET_KEY=test-secret-key-for-testing-only-do-not-use-in-production
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only-do-not-use-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# Redis Configuration (test instance)
REDIS_URL=redis://localhost:6379/1

# Logging Configuration
LOG_LEVEL=WARNING
LOG_FORMAT=json

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000

# Disable external services for testing
PROMETHEUS_ENABLED=false
METRICS_PORT=8002

# AWS Configuration (disabled for tests)
AWS_ACCESS_KEY_ID=test-key
AWS_SECRET_ACCESS_KEY=test-secret
AWS_REGION=us-west-2